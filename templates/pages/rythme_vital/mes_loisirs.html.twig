{% extends 'base.html.twig' %}

{% block title %}Waou - Liste de mes loisirs
{% endblock %}
{% block stylesheets %}
	{{ parent() }}
	{{ encore_entry_link_tags('list_calendrier') }}
{% endblock %}
{% block body %}
	<section class="container-list-calendrier element-to-scroll">
		<div class="container-list row">
			<a class="button-add" href="{{ path('app_rythme_vital_ajouter_mes_loisirs') }}">Ajouter</a>
			<div class="container-texte-exemple">
				<h3>Dans cette rubrique indiquez le temps que vous souhaitez consacrer a vos loisirs personnels.<br>Exemple : “Aller courir 3 fois par semaines pour participer au marathon“</h3>
			</div>
			<table class="table table-striped">
				<thead>
					<tr class="six_columns">
						<th scope="col" class="intitule" style="width: 30%;">Intitulé</th>
						<th scope="col">Lundi</th>
						<th scope="col">Mardi</th>
						<th scope="col">Mercredi</th>
						<th scope="col">Jeudi</th>
						<th scope="col">Vendredi</th>
						<th scope="col">Samedi</th>
						<th scope="col">Dimanche</th>
						<th scope="col">Action</th>
					</tr>
				</thead>
				<tbody>
					{% for entry in lstMesLoisirs %}
						<tr class="six_columns">
							<th scope="row">{{ entry.objet }}</th>
							<td>{{ entry.lundi.format('H:i') }}</td>
							<td>{{ entry.mardi.format('H:i') }}</td>
							<td>{{ entry.mercredi.format('H:i') }}</td>
							<td>{{ entry.jeudi.format('H:i') }}</td>
							<td>{{ entry.vendredi.format('H:i') }}</td>
							<td>{{ entry.samedi.format('H:i') }}</td>
							<td>{{ entry.dimanche.format('H:i') }}</td>
							<td>
								<a class="delete-button" href="{{ path('app_rythme_vital_mes_loisirs_delete', {'id' : entry.id}) }}" onclick="return confirm('Voulez-vous vraiment supprimer ce loisir ?')">
									<img style="width: 50%;" src="{{ asset('img/icon-croix.png') }}" alt="Supprimer">
								</a>
								<a href="{{ path('app_rythme_vital_modifier_mes_loisirs', {'id' : entry.id}) }}">
									<img style="width: 40%;" src="{{ asset('img/icon-modifier.png') }}" alt="modifier">
								</a>
							</td>
						</tr>
					{% endfor %}
				</tbody>
			</table>
		</div>
	</section>
{% endblock %}
